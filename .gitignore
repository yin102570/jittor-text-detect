# 虚拟环境（本地运行用，无需提交）
env/
detectgpt-env/
venv/
*.venv/
# 模型文件
*.bin
*.pth
*.ckpt
.cache/huggingface/
models/

# 数据集
datasets/
*.zip
*.tar.gz
*.jsonl
# 实验结果与临时文件
tmp_results/
results/
data/  # 数据集目录（如果数据集很大，不建议提交；如果是小数据集可删除这行）

# 模型缓存（Hugging Face 下载的模型，国内用户可保留本地，无需提交）
cache/
.hf-cache/
transformers_cache/

# Python 编译缓存与日志
__pycache__/
*.py[cod]
*$py.class
*.log
*.tmp

# 系统自动生成的文件
.DS_Store/  # Mac 系统
Thumbs.db  # Windows 系统
.vscode/    # VS Code 配置（如果需要共享配置可删除这行）
.idea/      # PyCharm 配置（如果需要共享配置可删除这行）

# 其他无用文件
*.zip
*.tar.gz
*.csv
*.jsonl
!args.json  # 例外：保留 args.json（如果需要提交实验参数可加这行，否则删除）